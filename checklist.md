# Work Order

[ ] Write Postgres migrations for: authors, works, versions, chunks, ingest_state.
[ ] Write the ingest runner that indexes 200 PRI Arabic works end-to-end.
[ ] Add scripts: init-search and alias-swap.
[ ] Implement 3 FastAPI endpoints: search + chunk fetch + filters metadata.
[ ] Build the minimal Next.js UI wired to those endpoints.
[ ] Add embeddings + hybrid on the subset.
[ ] Scale to full RELEASE.